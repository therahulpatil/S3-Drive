# Production Node.js Backend API Container
FROM node:20-alpine

WORKDIR /app

# Copy package manifests & install production dependencies
COPY package.json package-lock.json ./
RUN npm ci --only=production

# Copy server code
COPY server.js .env* ./

# Expose API Port
EXPOSE 5000

CMD ["node", "server.js"]
